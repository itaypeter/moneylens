[build]
builder = "nixpacks"

[build.buildCommand]
cmd = "cd frontend && npm install && npm run build && cd ../backend && npm install"

[deploy]
startCommand = "cd backend && node server.js"
healthcheckPath = "/health"
